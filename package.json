{
  "name": "cameron-macek",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site for Cameron Macek \u2014 operations, AI implementation, and internal tools.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build && node scripts/check-output.mjs",
    "build:fast": "astro build && node scripts/check-output.mjs",
    "preview": "astro preview",
    "fonts": "node scripts/sync-fonts.mjs",
    "astro": "astro",
    "check:output": "node scripts/check-output.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "@astrojs/sitemap": "^3.7.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/source-serif-4": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "typescript": "^5.7.2"
  }
}
